{
  "name": "workspace",
  "version": "1.0.0",
  "scripts": {
    "build": "cd artifacts/haris-portfolio && npm install && npm run build",
    "deploy": "npx wrangler pages deploy artifacts/haris-portfolio/dist"
  }
}
